### PIANO × MirrorMind TODO

- Wire SkillExecutor hooks to real integrations: CI runner, graph DB client (Neo4j live), HF pipelines for fine_tune/apply_lora/run_inference (scripts/configs provided; env overrides ready).
- Extend multi-step coordination (retries, role/rule/meme metrics) and broaden trajectory tests.
- Broaden unit tests for executor side effects and controller decisions with mocked policies.
